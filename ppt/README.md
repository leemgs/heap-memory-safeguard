# HMS presentation

`HMS_TECS_presentation.pptx` is the Korean presentation deck for the revised
simulation-backed HMS paper.

Regenerate it with:

```bash
PYTHONPATH=/tmp/hms-pptx python ppt/generate_presentation.py
```

The deck intentionally distinguishes synthetic model outputs from future Android
device measurements.
